import React from 'react';
import WorkGrid from '../components/work/WorkGrid';

export default function Work() {
  return (
    <div style={{ paddingTop: '80px', background: 'var(--off-white)', minHeight: '100vh' }}>
      <WorkGrid />
    </div>
  );
}
